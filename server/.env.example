MONGODB_URI=mongodb://127.0.0.1:27017/task_tracker
PORT=5000
# Extra origins (optional). Local + production Render frontend are always allowed in code.
CLIENT_ORIGIN=http://localhost:5173,https://editflows-manager-frontend.onrender.com
